version: '3.8'
services:
  postgres:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=12345
    volumes:
      - test:/var/lib/postgres/data
volumes:
  test: